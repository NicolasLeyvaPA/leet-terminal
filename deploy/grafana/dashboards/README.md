Place JSON dashboard files in this folder to be auto-provisioned into Grafana.

Examples you might add:
- Postgres DB viewer dashboard (table list, sizes, connections)
- Traffic dashboard (queries/sec, response codes) sourced from Prometheus
