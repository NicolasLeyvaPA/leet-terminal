# Example env for deployment - copy to .env and fill secrets before use
POSTGRES_USER=user_admin
POSTGRES_PASSWORD=changeme
USERS_DSN=postgres://user_admin:changeme@postgres:5432/users_db?sslmode=disable

# Web app admin seed
WEB_ADMIN_USERNAME=admin
WEB_ADMIN_EMAIL=admin@leet-terminal.com
WEB_ADMIN_PASSWORD=changeme

# Grafana admin password
GF_SECURITY_ADMIN_PASSWORD=changeme

# Grafana URL for embedding
REACT_APP_GRAFANA_URL=http://localhost:3000
